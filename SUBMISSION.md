# 제출 기록

## 공개 결과물 주소

[공개 포트폴리오](https://intro-my-nine.vercel.app/)

2026-09-16 실제 GET 응답 200 확인. 아래 로컬 보완은 아직 재배포하지 않았습니다.

## 소스 주소

미확정 — 제공된 주소는 배포 주소입니다. 공개 여부와 제출 허용이 확인된 소스 저장소 URL이 필요합니다. 개인 계정 ID가 포함된 기존 원격 주소는 제출 문서에 옮기지 않았습니다.

## 짧은 확인 방법

1. 공개 결과물 주소의 `나만의 공간`으로 이동합니다.
2. 세 단계 안에 패스키 등록 또는 패스키 로그인을 수행합니다.
3. 로그인 후 가상 개인 카드, 패스키 목록, 로그아웃 버튼이 보이는지 확인합니다.
4. 패스키 취소, 미지원 브라우저, 마지막 패스키 삭제에서는 자연스러운 오류 안내가 보이는지 확인합니다.

## 제출 전 남은 점검

- 실제 완료: 운영 무인증 `GET /api/private` → 401 / `{"error":"인증이 필요합니다."}`.
- 확인 전: 성공 verify 재사용, 패스키 A/B 중 A 삭제 후 B 로그인, 비운영 테스트 사용자의 교차 credential/challenge 거부. 정확한 실행 순서와 기대 응답은 `AUTH_IMPLEMENTATION.md`에 있습니다.
- 운영 Vercel 환경 변수의 `INITIAL_SETUP=false`, `TEST_MODE=false`는 관리 화면에서 확인해야 합니다. status의 `setupAvailable:false`만으로 변수 값을 증명할 수 없습니다.
- 재배포 후 개인정보 보완, 추가 패스키 UI와 인증 응답의 `Cache-Control: no-store`를 재확인합니다.

## 개인정보 및 비밀값 점검 범위

- 이름 `김가빈`은 사용자의 명시적인 요청에 따라 공개 정보로 유지합니다. 개인 Notion 문서 URL과 package.json의 개인 계정 저장소 메타데이터를 제거했습니다.
- `.env.example`은 placeholder만 포함합니다. 실제 환경 파일, `.vercel/`, `data/`, `node_modules/`, `.git/` 및 원본 네트워크 로그는 제출하지 않습니다. `.gitignore`만으로 폴더 전체 ZIP에서 제외되지는 않습니다.
- assets PNG 6개와 HTML 삽입 PNG 4개를 육안 점검했습니다. 이름·연락처·학생번호·QR·브라우저 프로필은 발견하지 않았습니다. 계획 화면은 실제 기록 여부가 확인되지 않아 기존 가상 학습 이미지로 대체했습니다.
- assets 6개는 IHDR/caBX/IDAT/IEND, 삽입 이미지 4개는 IHDR/IDAT/IEND 청크였습니다. EXIF/텍스트 청크는 없고 caBX에는 AI 생성 출처(C2PA)가 있습니다. 메타데이터 문자열 검사에서 GPS/개인 작성자/개인키 표시는 발견하지 않았으며 생성 출처 정보는 유지했습니다.
- 과거 SQLite 파일 2개가 로컬 `data/`에 남아 있습니다. 읽기 전용으로 테이블/행 수만 확인했고 내용을 제출하지 않았습니다. 운영 코드에서는 사용하지 않습니다. 삭제하지 않았으므로 작업 폴더 전체를 제출하면 안 됩니다.
- Git 과거 이력과 운영 Neon 저장 데이터 전체는 개인정보 부재를 검증하지 못했습니다. 현재 운영 페이지에도 로컬 개인정보 보완이 아직 반영되지 않았으므로 “허용한 공개 이름 외 개인정보/비밀값 없음”은 아직 완료로 판정하지 않습니다.

## 로컬 보완 검증

- JavaScript 13개 파일 문법 검사 통과, diff 공백 검사 통과.
- `resetSessionOnPageLoad`와 `api/_lib/db.js`가 변경되지 않았음을 원본과 비교 확인했습니다.
- package.json의 실행 스크립트는 start/dev만 유지합니다.
- 추적 중인 텍스트 파일에서 기존 개인 식별 문자열, 이메일, 전화번호, 대표 API 키 및 개인키 패턴을 검색했고 일치 항목은 없었습니다. 패턴 검색만으로 모든 형태의 개인정보 부재를 보장하지는 않습니다.

## AI와 내 판단

1. AI에게 맡긴 일: WebAuthn 서버 검증과 UI 연결의 구현 초안.
2. 내가 직접 판단한 일: 공개 정보의 범위, 비공개 자료의 가상화, 운영 설정과 수동 검증 범위.
3. AI 말을 따르지 않은 일: 클라이언트 숨김 방식, 비밀번호 fallback, localStorage 인증 상태는 사용하지 않음.
